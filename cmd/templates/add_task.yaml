# add_task.yaml
task:
  name: "example_task" # 任务名
  target: "/path/to/target/directory" # 目标目录路径
  backup: "/path/to/backup/directory" # 备份存放路径
  retention:
    count: 3 # 保留数量
    days: 7 # 保留天数(配置为0时禁用)
  backup_dir_name: "" # 备份目录名(配置为""时,默认获取目标目录的目录名作为备份目录名)
  no_compression: false # 是否禁用压缩
