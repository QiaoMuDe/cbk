用法：cbk complete -type <类型>

描述：
  生成指定类型的自动补全脚本。

参数：
  -type <类型>                必需。指定要生成的自动补全脚本的类型。可选值：bash。

示例：
  cbk complete -type bash
  为当前命令行工具生成 bash 类型的自动补全脚本。

注意：
  1. 类型：必须指定一个有效的类型，目前支持 bash 和 powershell。
  2. 安装：生成的自动补全脚本需要手动安装到相应的命令行环境中。